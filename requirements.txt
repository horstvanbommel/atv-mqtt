# Hard-pinned dependencies for scripts/atv_mqtt_bridge.py
# Regenerated on 2026-09-07 (Python 3.9.6, /usr/bin/python3) to bump pyatv
# 0.17.0 -> 0.18.0 (fixes power-state subscription being dropped when the
# initial Companion SystemStatus fetch fails, e.g. because the Apple TV is
# off/in standby — smarthome-scripts-7et).
# To reproduce exactly on recovery:
#   python3 -m pip install -r requirements.txt
#
# Direct dependencies: paho-mqtt, pyatv
# Everything else below is a transitive runtime dependency, pinned for full reproducibility.

paho-mqtt==2.1.0
pyatv==0.18.0

aiohappyeyeballs==2.6.1
aiohttp==3.13.5
aiosignal==1.4.0
annotated-types==0.7.0
async-timeout==5.0.1
attrs==26.1.0
certifi==2026.7.22
cffi==2.0.0
chacha20poly1305-reuseable==0.13.2
charset-normalizer==3.5.1
cryptography==50.0.1
frozenlist==1.8.0
idna==3.19
ifaddr==0.2.0
miniaudio==1.71
multidict==6.7.1
propcache==0.4.1
protobuf==6.33.6
pycparser==2.23
pydantic==2.13.5
pydantic-core==2.46.5
requests==2.32.5
six==1.17.0
srptools==1.0.1
tabulate==0.9.0
tinytag==2.3.1
typing-extensions==4.16.0
typing-inspection==0.4.2
urllib3==2.6.3
yarl==1.22.0
zeroconf==0.148.0
